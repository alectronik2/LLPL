namespace HAL { 
    func outb(port: uint, value: u8) {
        asm("outb %0, %w1" : : "a"(value), "Nd"(port))
    }

    func inb(port: uint) -> u8 {
        let value: char = 0
        asm("inb %w1, %0" : "=a"(value) : "Nd"(port))
        value
    }

    func halt() {
        asm("hlt")
    }

    func disable_interrupts() {
        asm("cli")
    }
}