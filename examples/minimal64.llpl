// Absolutely minimal 64-bit kernel

func kernel_main() {
    let vga: uint* = 753664 as uint*  // 0xB8000

    // Write "LLPL" in white
    vga[0] = 0x0f4c0f4c  // "LL"
    vga[1] = 0x0f4c0f50  // "PL"

    // Infinite loop
    while true {
        // Hang forever
    }
}
