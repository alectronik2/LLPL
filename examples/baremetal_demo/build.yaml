# Build config for the GRUB/Multiboot2 bare-metal demo - replaces the old
# Makefile. Run with ../../tools/llplbuild/llplbuild (build|run|clean|configs)
# from within this directory. See tools/llplbuild/README.md for the tool
# itself, and the "test-persistence.sh" script alongside this file for the
# multi-boot VFS-persistence integration test (kept as a standalone script,
# not part of this pipeline - see that script's own header comment).

project: llpl-baremetal-demo
entry: kernel.llpl
llpl_compiler: ../../llpl

toolchain:
  nasm: nasm
  cc: gcc
  ld: ld
  qemu: qemu-system-x86_64

common_cflags:
  - -m64
  - -ffreestanding
  - -nostdlib
  - -nostdinc
  - -fno-builtin
  - -fno-stack-protector
  - -fno-omit-frame-pointer
  - -fno-optimize-sibling-calls
  - -Wall
  - -Wextra
  - -I.
  - -mcmodel=kernel
  - -fno-pie
  - -mno-red-zone
  - -mno-sse
  - -mno-sse2
  - -mno-mmx
  - -mno-80387

default_config: final
configurations:
  debug:
    cflags: [-O0, -g]
  final:
    cflags: [-O2]

asm_sources:
  - src: boot64.asm
    output: boot.o

c_sources:
  - path: kernel.c
    include_dirs: [../../runtime]
  - path: ../../runtime/runtime.c
    include_dirs: [../../runtime]

link:
  output: kernel.bin
  script: linker64.ld
  ldflags: [-m, elf_x86_64, -nostdlib]
  objects: [boot.o, kernel.o, runtime.o]

extra_links:
  - name: userapp
    asm_sources:
      - src: userapp/tests/userapp.asm
        output: userapp.o
    link:
      output: userapp.elf
      script: userapp/linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -static]
      objects: [userapp.o]
  - name: llpl_userapp
    llpl_sources:
      - src: userapp/tests/llpl_userapp.llpl
        c_output: userapp/tests/llpl_userapp.c
        output: userapp/tests/llpl_userapp.o
        include_dirs: [., ../../runtime]
        cflags: [-mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: llpl_userapp.elf
      script: userapp/linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -static, --gc-sections]
      objects: [userapp/tests/llpl_userapp.o]
  - name: libsys
    llpl_sources:
      - src: libsys/libsys.llpl
        c_output: libsys/libsys.c
        output: libsys/libsys.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=default, -mcmodel=large, -ffunction-sections, -fdata-sections]
    c_sources:
      # A handful of runtime.c's helpers reimplemented standalone here
      # (see runtime_shim.c's own doc comment) rather than linking the
      # whole of runtime.c in: libsys.llpl's own generated code (via
      # prelude.llpl's always-compiled-in String/class boilerplate)
      # already references most of runtime.c's surface internally, which
      # makes --gc-sections unable to strip any of it once the real
      # runtime.c is linked in at all - pulling in the whole file to fix
      # one missing symbol (llpl_strcmp, needed because some consumer's
      # `char* == null` comparison happens to lower to it - see wm.llpl,
      # where the optimizer didn't manage to fold that away the way it
      # apparently does for tetris.elf/atad.elf's own such comparisons)
      # made libsys.so balloon past the VFS's 32KB max file size (see
      # vfs.llpl's MAX_DIRECT_BLOCKS).
      - path: libsys/runtime_shim.c
        output: libsys/runtime_shim.o
        cflags: [-fPIC, -mcmodel=large]
    link:
      output: libsys.so
      script: libsys/linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -soname, libsys.so, --hash-style=sysv, --gc-sections, --version-script, libsys/exports.map]
      objects: [libsys/libsys.o, libsys/runtime_shim.o]
  - name: dyn_userapp
    llpl_sources:
      - src: userapp/tests/dyn_userapp.llpl
        c_output: userapp/tests/dyn_userapp.c
        output: userapp/tests/dyn_userapp.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: dyn_userapp.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/dyn_userapp.o, libsys.so]
  - name: memtest
    llpl_sources:
      - src: userapp/tests/memtest.llpl
        c_output: userapp/tests/memtest.c
        output: userapp/tests/memtest.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: memtest.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/memtest.o, libsys.so]
  - name: cat
    llpl_sources:
      - src: userapp/tests/cat.llpl
        c_output: userapp/tests/cat.c
        output: userapp/tests/cat.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: cat.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/cat.o, libsys.so]
  - name: spawnwait
    llpl_sources:
      - src: userapp/tests/spawnwait.llpl
        c_output: userapp/tests/spawnwait.c
        output: userapp/tests/spawnwait.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: spawnwait.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/spawnwait.o, libsys.so]
  - name: execchain
    llpl_sources:
      - src: userapp/tests/execchain.llpl
        c_output: userapp/tests/execchain.c
        output: userapp/tests/execchain.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: execchain.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/execchain.o, libsys.so]
  - name: echoserver
    llpl_sources:
      - src: userapp/tests/echoserver.llpl
        c_output: userapp/tests/echoserver.c
        output: userapp/tests/echoserver.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: echoserver.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/echoserver.o, libsys.so]
  - name: ipcdemo
    llpl_sources:
      - src: userapp/tests/ipcdemo.llpl
        c_output: userapp/tests/ipcdemo.c
        output: userapp/tests/ipcdemo.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: ipcdemo.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/ipcdemo.o, libsys.so]
  - name: echod
    llpl_sources:
      - src: userapp/tests/echod.llpl
        c_output: userapp/tests/echod.c
        output: userapp/tests/echod.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: echod.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/echod.o, libsys.so]
  - name: echoclient
    llpl_sources:
      - src: userapp/tests/echoclient.llpl
        c_output: userapp/tests/echoclient.c
        output: userapp/tests/echoclient.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: echoclient.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/echoclient.o, libsys.so]
  - name: shmserver
    llpl_sources:
      - src: userapp/tests/shmserver.llpl
        c_output: userapp/tests/shmserver.c
        output: userapp/tests/shmserver.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: shmserver.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/shmserver.o, libsys.so]
  - name: shmclient
    llpl_sources:
      - src: userapp/tests/shmclient.llpl
        c_output: userapp/tests/shmclient.c
        output: userapp/tests/shmclient.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: shmclient.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/shmclient.o, libsys.so]
  - name: atad
    llpl_sources:
      - src: userapp/atad.llpl
        c_output: userapp/atad.c
        output: userapp/atad.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: atad.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/atad.o, libsys.so]
  - name: netd
    llpl_sources:
      - src: userapp/netd.llpl
        c_output: userapp/netd.c
        output: userapp/netd.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: netd.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/netd.o, libsys.so]
  - name: ping
    llpl_sources:
      - src: userapp/ping.llpl
        c_output: userapp/ping.c
        output: userapp/ping.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: ping.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/ping.o, libsys.so]
  - name: traceroute
    llpl_sources:
      - src: userapp/traceroute.llpl
        c_output: userapp/traceroute.c
        output: userapp/traceroute.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: traceroute.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/traceroute.o, libsys.so]
  - name: tetris
    llpl_sources:
      - src: userapp/tetris.llpl
        c_output: userapp/tetris.c
        output: userapp/tetris.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: tetris.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tetris.o, libsys.so]
  - name: wm
    llpl_sources:
      - src: userapp/wm.llpl
        c_output: userapp/wm.c
        output: userapp/wm.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: wm.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/wm.o, libsys.so]
  - name: winball1
    llpl_sources:
      - src: userapp/tests/winball1.llpl
        c_output: userapp/tests/winball1.c
        output: userapp/tests/winball1.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: winball1.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/winball1.o, libsys.so]
  - name: winball2
    llpl_sources:
      - src: userapp/tests/winball2.llpl
        c_output: userapp/tests/winball2.c
        output: userapp/tests/winball2.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: winball2.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/tests/winball2.o, libsys.so]
  - name: filebrowser
    llpl_sources:
      - src: userapp/filebrowser.llpl
        c_output: userapp/filebrowser.c
        output: userapp/filebrowser.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: filebrowser.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/filebrowser.o, libsys.so]
  - name: editor
    llpl_sources:
      - src: userapp/editor.llpl
        c_output: userapp/editor.c
        output: userapp/editor.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: editor.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/editor.o, libsys.so]
  - name: terminal
    llpl_sources:
      - src: userapp/terminal.llpl
        c_output: userapp/terminal.c
        output: userapp/terminal.o
        include_dirs: [., ../../runtime]
        cflags: [-fPIC, -fvisibility=hidden, -mcmodel=large, -ffunction-sections, -fdata-sections]
    link:
      output: terminal.elf
      script: userapp/dyn_linker.ld
      ldflags: [-m, elf_x86_64, -nostdlib, -shared, -e, _start, --hash-style=sysv, --gc-sections]
      objects: [userapp/terminal.o, libsys.so]

# Includes two small files loaded as multiboot2 modules (grub.cfg's
# `module2` directive) purely to give tmpfs.llpl something real to
# expose; their exact content is asserted by TmpFS.selftest().
package:
  output: kernel.iso
  actions:
    - mkdir: isodir/boot/grub
    - copy: {from: kernel.bin, to: isodir/boot/kernel.bin}
    - copy: {from: userapp.elf, to: isodir/boot/userapp.elf}
    - copy: {from: llpl_userapp.elf, to: isodir/boot/llpl_userapp.elf}
    - copy: {from: libsys.so, to: isodir/boot/libsys.so}
    - copy: {from: dyn_userapp.elf, to: isodir/boot/dyn_userapp.elf}
    - copy: {from: memtest.elf, to: isodir/boot/memtest.elf}
    - copy: {from: cat.elf, to: isodir/boot/cat.elf}
    - copy: {from: spawnwait.elf, to: isodir/boot/spawnwait.elf}
    - copy: {from: execchain.elf, to: isodir/boot/execchain.elf}
    - copy: {from: echoserver.elf, to: isodir/boot/echoserver.elf}
    - copy: {from: ipcdemo.elf, to: isodir/boot/ipcdemo.elf}
    - copy: {from: echod.elf, to: isodir/boot/echod.elf}
    - copy: {from: echoclient.elf, to: isodir/boot/echoclient.elf}
    - copy: {from: shmserver.elf, to: isodir/boot/shmserver.elf}
    - copy: {from: shmclient.elf, to: isodir/boot/shmclient.elf}
    - copy: {from: atad.elf, to: isodir/boot/atad.elf}
    - copy: {from: netd.elf, to: isodir/boot/netd.elf}
    - copy: {from: ping.elf, to: isodir/boot/ping.elf}
    - copy: {from: traceroute.elf, to: isodir/boot/traceroute.elf}
    - copy: {from: tetris.elf, to: isodir/boot/tetris.elf}
    - copy: {from: wm.elf, to: isodir/boot/wm.elf}
    - copy: {from: winball1.elf, to: isodir/boot/winball1.elf}
    - copy: {from: winball2.elf, to: isodir/boot/winball2.elf}
    - copy: {from: filebrowser.elf, to: isodir/boot/filebrowser.elf}
    - copy: {from: editor.elf, to: isodir/boot/editor.elf}
    - copy: {from: terminal.elf, to: isodir/boot/terminal.elf}
    - write:
        to: isodir/boot/hello.txt
        content: "Hello from a GRUB module!"
    - write:
        to: isodir/boot/readme.txt
        content: "This file was never written to the VFS disk - TmpFS reads it directly out of the memory GRUB already loaded it into."
    - write:
        to: isodir/boot/grub/grub.cfg
        content: |
          set timeout=0
          set default=0
          menuentry "LLPL Bare-Metal Demo" {
              multiboot2 /boot/kernel.bin
              module2 /boot/hello.txt hello.txt
              module2 /boot/readme.txt readme.txt
              module2 /boot/userapp.elf userapp.elf
              module2 /boot/llpl_userapp.elf llpl_userapp.elf
              module2 /boot/libsys.so libsys.so
              module2 /boot/dyn_userapp.elf dyn_userapp.elf
              module2 /boot/memtest.elf memtest.elf
              module2 /boot/cat.elf cat.elf
              module2 /boot/spawnwait.elf spawnwait.elf
              module2 /boot/execchain.elf execchain.elf
              module2 /boot/echoserver.elf echoserver.elf
              module2 /boot/ipcdemo.elf ipcdemo.elf
              module2 /boot/echod.elf echod.elf
              module2 /boot/echoclient.elf echoclient.elf
              module2 /boot/shmserver.elf shmserver.elf
              module2 /boot/shmclient.elf shmclient.elf
              module2 /boot/atad.elf atad.elf
              module2 /boot/netd.elf netd.elf
              module2 /boot/ping.elf ping.elf
              module2 /boot/traceroute.elf traceroute.elf
              module2 /boot/tetris.elf tetris.elf
              module2 /boot/wm.elf wm.elf
              module2 /boot/winball1.elf winball1.elf
              module2 /boot/winball2.elf winball2.elf
              module2 /boot/filebrowser.elf filebrowser.elf
              module2 /boot/editor.elf editor.elf
              module2 /boot/terminal.elf terminal.elf
          }
    - run: "grub-mkrescue -o kernel.iso isodir"
      allow_failure: true

# The VFS's (vfs.llpl) backing store - built only if missing, so it
# persists across ordinary builds the same way real disk state should
# (only `clean` wipes it). Must match vfs.llpl's DISK_SECTORS constant
# (16MB / 512-byte sectors).
persistent_files:
  - path: disk.img
    create: "dd if=/dev/zero of=disk.img bs=1M count=16"

# QEMU assigns the extra `-drive ...,if=ide` (with no explicit index) as
# the primary IDE master alongside `-cdrom` - verified, not merely
# assumed, but still just "this is what QEMU happens to do today", the
# same "trust the target" spirit as the rest of this demo's hardware
# handling (see ata.llpl).
run:
  args: ["-cdrom", "kernel.iso", "-drive", "file=disk.img,format=raw,if=ide", "-serial", "stdio", "-m", "256"]
